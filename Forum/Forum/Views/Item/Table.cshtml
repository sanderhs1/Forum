@model IEnumerable<Forum.Models.Listing>

<div class="container">
    <h1>@ViewBag.CurrentViewName</h1>
    <table class='table'>
            <!-- Legge inn bilde som en variabel--> 
        @foreach (var listings in Model)
        {
            <tr>
                <td>@listings.ListingId</td>
                <td>@listings.Name</td>
                <td>@listings.Price.ToString("0.00 NOK")</td>
                <td></td>
            </tr>
        }
    </table>
    </div>
